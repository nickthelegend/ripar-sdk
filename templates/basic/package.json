{
  "name": "{{handle}}",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "ripar dev agent.ts",
    "start": "node agent.ts",
    "quote": "ripar quote http://localhost:4021/echo",
    "doctor": "ripar doctor"
  },
  "dependencies": {
    "@ripar/sdk": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  },
  "engines": { "node": ">=22.6" }
}
